import { defineComponent, ref,  watch, computed } from "vue";

export default defineComponent({
    name: 'gasesMedicion',
    props: {
        info: String
    },
    components:{
    },
    setup() {
        return {

        }
    }
})